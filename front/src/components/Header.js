//import liraries
import React, { Component } from 'react';

// create a component
const Header = () => {
    return (
        <div style={styles.container}>
            <h1> Header </h1>
        </div>
    );
};

export default Header;
